# ComfyUI-LLM-Node
ComfyUI custom node (CSYIDC-RefDescribe) that describes up to 15 reference images with a vision-language model using any OpenAI-compatible endpoint, then merges the descriptions into one complete text output. Provider-agnostic, no bundled API key or defaults.
